---
driver:
  name: docker

provisioner:
  name: chef_zero

platforms:
  - name: ubuntu-14.04

suites:
  - name: server
    run_list:
      - recipe[nginx]
    attributes:
      nginx:
        skip_default_site: false
