server {
  listen <%= @listen %>;
  server_name <%= @host %>;

  location / {
    root <%= @root %>;
    <% if @location %>
    <%= @location %>;
    <% end %>
    index <%= @index %>;
  }

  # Deny access to .htaccess & .htpasswd files
  location ~ /\.ht {
    deny all;
  }
}
